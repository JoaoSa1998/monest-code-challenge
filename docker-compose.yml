services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: monest-api
    env_file:
      - ./api/.env
    ports:
      - "3000:3000"
    restart: unless-stopped
